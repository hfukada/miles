services:
  miles-api:
    build:
      context: .
      args:
        GIT_HASH: ${GIT_HASH:-latest}
        BUILD_TIME: ${BUILD_TIME:-}
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
